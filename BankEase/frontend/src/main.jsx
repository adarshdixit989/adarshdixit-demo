import React,{useState}from'react';import{createRoot}from'react-dom/client';import{LogIn,UserPlus,Wallet,Send,ArrowDownToLine,ArrowUpFromLine,History,ShieldCheck,LogOut}from'lucide-react';import'./style.css';
const API=import.meta.env.VITE_API_URL||'https://bankease-api.onrender.com';
function App(){const[token,setToken]=useState(localStorage.getItem('token')||'');const[user,setUser]=useState(JSON.parse(localStorage.getItem('user')||'null'));const[mode,setMode]=useState('login');const[form,setForm]=useState({name:'',email:'',password:''});const[msg,setMsg]=useState('');const[loading,setLoading]=useState(false);
async function auth(e){e.preventDefault();setLoading(true);setMsg('');try{const url=mode==='login'?'/api/auth/login':'/api/auth/register';const r=await fetch(API+url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)throw Error(d.message||'Request failed');if(mode==='register'){setMsg('Registration successful. Please login.');setMode('login')}else{localStorage.setItem('token',d.token);localStorage.setItem('user',JSON.stringify(d));setToken(d.token);setUser(d)}}catch(x){setMsg(x.message)}finally{setLoading(false)}}function logout(){localStorage.clear();setToken('');setUser(null)}if(!token)return <div className="auth"><div className="brand"><div className="logo">B</div><div><h1>BankEase</h1><p>Secure digital banking</p></div></div><div className="card"><h2>{mode==='login'?'Welcome back':'Create your account'}</h2><p className="muted">{mode==='login'?'Sign in to manage your money.':'Open your BankEase account in seconds.'}</p><form onSubmit={auth}>{mode==='register'&&<input placeholder="Full name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/>}<input type="email" placeholder="Email address" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/><input type="password" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/><button disabled={loading}>{mode==='login'?<><LogIn/>Sign in</>:<><UserPlus/>Create account</>}</button></form>{msg&&<div className="msg">{msg}</div>}<button className="link" onClick={()=>{setMode(mode==='login'?'register':'login');setMsg('')}}>{mode==='login'?'New to BankEase? Create account':'Already registered? Sign in'}</button></div><div className="trust"><ShieldCheck/> JWT secured • REST API • Spring Boot</div></div>;
return <div className="app"><header><div className="brand"><div className="logo">B</div><div><h1>BankEase</h1><span>Digital Banking</span></div></div><button className="logout" onClick={logout}><LogOut/> Logout</button></header><main><section className="hero"><div><p className="eyebrow">OVERVIEW</p><h2>Good to see you, {user?.name||'Customer'} 👋</h2><p className="muted">Your banking, simple and secure.</p></div></section><div className="grid"><div className="balance"><Wallet/><span>Available Balance</span><strong>₹0.00</strong><small>Connect an account to see live balance</small></div><div className="quick"><h3>Quick actions</h3><div className="actions"><button><Send/>Send money</button><button><ArrowDownToLine/>Deposit</button><button><ArrowUpFromLine/>Withdraw</button><button><History/>Transactions</button></div></div></div><div className="notice"><ShieldCheck/><div><b>BankEase security</b><p>Authentication is protected with JWT. Your API is powered by Spring Boot and can also be tested through Swagger.</p></div></div></main></div>}createRoot(document.getElementById('root')).render(<App/>);